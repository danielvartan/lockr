#' @param password (optional) only for protected keys. A string specifying
#'   the password to read the private key. Avoid typing passwords on the
#'   console, use [askpass()][askpass::askpass()] instead (default: `NULL`).
